{
  "name": "@cleanlink/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CleanLink API — Hono.js на Cloudflare Workers (D1 + KV + R2 + Queues + Workers AI).",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit && wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 execute cleanlink-db --local --file=../migrations/0001_init.sql && wrangler d1 execute cleanlink-db --local --file=../migrations/0002_company_views.sql",
    "db:migrate:remote": "wrangler d1 execute cleanlink-db --remote --file=../migrations/0001_init.sql && wrangler d1 execute cleanlink-db --remote --file=../migrations/0002_company_views.sql",
    "db:seed:local": "wrangler d1 execute cleanlink-db --local --file=../migrations/0003_seed_demo_data.sql",
    "db:seed:remote": "wrangler d1 execute cleanlink-db --remote --file=../migrations/0003_seed_demo_data.sql"
  },
  "dependencies": {
    "hono": "4.13.7",
    "otplib": "12.0.1",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260702.1",
    "eslint": "9.39.5",
    "prettier": "3.9.6",
    "typescript": "5.9.3",
    "typescript-eslint": "8.69.0",
    "vitest": "3.2.7",
    "wrangler": "3.114.17"
  }
}
