import PageStub from '../components/common/PageStub';

/** `/` — главный экран поиска (F2). TODO(frontend): герой + поле города + чипсы городов. */
export default function HomePage() {
  return (
    <PageStub
      title="Найдите клининговую компанию в своём городе"
      description="Сравнивайте рейтинг, цены и отзывы — и закажите уборку в пару кликов"
    />
  );
}
