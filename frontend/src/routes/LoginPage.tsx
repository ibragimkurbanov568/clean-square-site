import PageStub from '../components/common/PageStub';

/** `/login` — вход (F1). Использует useAuth().login(). */
export default function LoginPage() {
  return (
    <PageStub
      title="Вход в CleanLink"
      description="TODO(frontend): форма email/пароль, POST /api/auth/login через useAuth()."
    />
  );
}
