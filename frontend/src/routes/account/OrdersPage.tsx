import PageStub from '../../components/common/PageStub';

/** `/account/orders` — заказы клиента с фильтром по статусу (F12). Использует useOrders(). */
export default function OrdersPage() {
  return (
    <PageStub
      title="Мои заказы"
      description="TODO(frontend): useOrders({ role: 'client' }) — GET /api/orders?status=..."
    />
  );
}
