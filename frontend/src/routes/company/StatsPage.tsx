import PageStub from '../../components/common/PageStub';

/** `/company/stats` (только company_verified) — статистика (F10). */
export default function StatsPage() {
  return (
    <PageStub title="Статистика" description="TODO(frontend): GET /api/company/stats?period=7|30|90." />
  );
}
