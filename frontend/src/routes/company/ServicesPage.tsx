import PageStub from '../../components/common/PageStub';

/** `/company/services` (только company_verified) — прайс-лист (F9 F10 не путать — это F5 источник). */
export default function ServicesPage() {
  return (
    <PageStub
      title="Услуги и цены"
      description="TODO(frontend): GET/POST/PATCH/DELETE /api/companies/:id/services, /api/services/:id."
    />
  );
}
