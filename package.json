{
  "name": "cleanlink",
  "version": "0.1.0",
  "private": true,
  "description": "CleanLink — агрегатор клининговых компаний по городам. Монорепозиторий: frontend (Vite+React+TS), backend (Hono на Cloudflare Workers), migrations (D1/SQLite).",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c yellow,cyan \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "test": "npm run test --workspace=backend && npm run test --workspace=frontend",
    "lint": "npm run lint --workspace=backend && npm run lint --workspace=frontend",
    "typecheck": "npm run typecheck --workspace=backend && npm run typecheck --workspace=frontend",
    "db:migrate:local": "npm run db:migrate:local --workspace=backend",
    "db:migrate:remote": "npm run db:migrate:remote --workspace=backend",
    "db:seed:local": "npm run db:seed:local --workspace=backend",
    "db:seed:remote": "npm run db:seed:remote --workspace=backend",
    "deploy": "npm run deploy --workspace=backend",
    "prepare": "husky"
  },
  "devDependencies": {
    "concurrently": "9.2.1",
    "husky": "^9.1.7"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
